plugins {
    id 'java'
    id 'maven-publish'
}
group 'net.mlk.jmson'

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId = 'net.mlk.jmson'
            artifactId = 'Jmson'
            version = '1.0'
        }
    }
}